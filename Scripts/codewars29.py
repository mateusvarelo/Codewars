def sort_by_length(arr):
     a = [palavra 
          for i in arr
          for j in range() 
          ]
     return arr
print(
    sort_by_length(
                ['beg', 'i', 'life', 'to']
                )
    )